import { Typography } from '@mui/material';

export const ParticleLog = () => {
    return (
        <Typography variant="h2" sx={{ marginTop: '100px' }}>
            This page is for Particle logs
        </Typography>
    );
};
